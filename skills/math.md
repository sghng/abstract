# Math

- We prefer using Typst for writing any math. The syntax of Typst is different
  from LaTeX.
- For Typst syntax conventions and common pitfalls, also read `typst.md`.
- When doing extensive math derivations, it's helpful to compile a Typst
  document to images then read the images and see if it meets your need.
